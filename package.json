{
  "name": "treino-app",
  "version": "1.0.0",
  "private": true,
  "description": "Registro de treino, cardio e acompanhamento corporal. Arquivo único, sem build, offline.",
  "scripts": {
    "test": "node --test \"tests/*.test.js\"",
    "serve": "npx serve .",
    "treino": "node tests/gerar-treino.js",
    "volume": "node tests/gerar-volume.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
